;;; template.el --- Template for adding additional packages or configs -*- lexical-binding: t; -*-

;;; Commentary:
;; Provides a comment on what the code does.
;; All keybinds should go in keybinds.el.

;;; Code:

;; use use-package to install using straight.el
;; (use-package multiple-cursors
;;   :straight t)

(provide 'template)

;;; template.el ends here
